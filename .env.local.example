# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
MODEL=gpt-4o-mini

# Supabase Configuration (required)
# Get these values from your Supabase project dashboard:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project
# 3. Go to Settings > API
# 4. Copy the values below

NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional: Supabase Service Role Key (for server-side operations that need elevated permissions)
# Only add this if you need admin-level database operations
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Optional: Base URL for shareable links (defaults to localhost in development)
# For production, set this to your deployed domain
NEXT_PUBLIC_BASE_URL=https://boardgame-sommelier-starter.vercel.app
